# Required: HTTP(S) proxy used for Telegram Web egress.
TELEGRAM_UPSTREAM_PROXY=http://proxy.example:3128

# Optional: comma-separated browser origins for direct cross-origin access.
PUBLIC_ORIGINS=

# Optional OpenAI-compatible Whisper endpoint.
WHISPER_UPSTREAM=
WHISPER_API_KEY=
